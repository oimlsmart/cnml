---
name: Schema request (new OIML Recommendation)
about: Add or update a per-Recommendation JSON Schema
labels: schema
---

## Recommendation

<!-- OIML Recommendation number and title. Example: R60 (Load cells). -->

## Source

<!-- Where is the canonical specification? OIML publication, edition,
     year. -->

## Fields to capture

<!-- List the certificate fields this Recommendation requires that
     are not in the shared `_core.yaml` schema. Examples:
       - Maximum number of divisions (n_max)
       - Minimum verification interval (v_min)
       - Accuracy class (A / B / C / D)
       - Temperature range
-->

## Test vectors

<!-- Three real published type-approval certificates for this
     Recommendation, if available. These will seed the test-vector
     corpus. -->

## Out of scope

<!-- What is explicitly NOT covered in this initial schema. Examples:
       - Multidimensional measurement ranges
       - Specific hardware interfaces
-->
